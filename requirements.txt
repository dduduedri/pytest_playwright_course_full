# Core test dependencies for the pytest + Playwright framework
pytest>=8.0.0
playwright>=1.44.0
pytest-playwright>=0.5.0
pytest-xdist>=3.5.0
pytest-html>=4.1.0
allure-pytest>=2.13.0
allure-python-commons>=2.13.0  # provides the top-level `import allure` module
